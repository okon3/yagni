# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent workspace: ignored by default, because tools write here. The binding is
# the exception, and PLAN.md is tracked outright — they are the only record of
# how this project is run, so pruning them has to be recoverable.
.claude/*
!.claude/orchestrate.md
